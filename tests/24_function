// Chapter 24
// Calls and Functions

// fun areWeHavingItYet() {
//     print "Yes we are!";
// }
// print areWeHavingItYet;


// var notAFunction = 123;
// notAFunction();

// fun sum(a, b, c) {
//     return a + b + c;
// }
// print 4 + sum(5, 6, 7);

// fun printarg(a) {
//     print a;
// }
// printarg("hello");

// fun a() { b(); }
// fun b() { c(); }
// fun c() {
//     c("too", "many");
// }
// a();

// fun a() { b(); }
// fun b() { c("too", "many"); }
// fun c() { print 1; }
// a();

// fun noReturn() {
//     print "Do stuff";
// }
// print noReturn();

// return "What?!";

fun fib(n) {
    if (n < 2) return n;
    return fib(n - 2) + fib(n - 1);
}
  
var start = clock();
print fib(35);
print clock() - start;